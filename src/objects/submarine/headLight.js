export default class HeadLight {
	constructor({scene, left, right, length}) {
		this.scene = scene;
		this.left = left;
		this.right = right;
		this.length = length;
	}

	flip(flipped) {
		if(flipped) {
			
		}
		else {
			
		}
	}

	update(time, delta) {

	}
}